# 国内 npm 镜像（淘宝官方迁移后的新域名 npmmirror.com）。
# 上线服务器（阿里云杭州节点）跑 `pnpm install` 走默认 registry.npmjs.org 经常
# ERR_SOCKET_TIMEOUT，build 单步要 15-20 分钟。锁这个镜像后整体 1-2 分钟。
#
# 本地 dev 不受影响：dev 一般用全局 ~/.npmrc 或者根本没配；项目级 .npmrc 优先
# 级最高，但 npmmirror 跟官方源完全同步，开发体验也不会变。
#
# 想换回官方 registry 直接删这行（或在命令上加 --registry=...）。
registry=https://registry.npmmirror.com/
